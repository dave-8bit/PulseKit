// Prisma v5 configuration file.
//
// This repository uses Prisma v5.x (prisma + @prisma/client).
// Prisma v5 does NOT support the Prisma 6/7 `prisma/config` API or
// ESM-style `dotenv/config` resolution in this environment.
//
// To keep compatibility, we intentionally remove custom config and let
// Prisma CLI rely on `schema.prisma` and DATABASE_URL from .env.
//
// Keeping this file (instead of deleting) avoids breaking any tooling that
// might reference it, but it exports nothing.
export {};

